---
title: 'CNOP Ask Me'
date: 2026-06-30
toc: false
---

<div class="cnop-ask-page">
<section class="cnop-ask-hero">
<p class="cnop-ask-kicker">Interactive CNOP notes</p>
<h1>CNOP Ask Me</h1>
<p>【AI 文本，暂未审核】Ask a question about CNOP-related concepts, workflows, or notes. Answers are distilled from a private corpus of 201 published papers and 47 theses covering predictability, targeted observation, ensemble prediction, and nonlinear optimization methods.</p>
<p>提出一个关于 CNOP 相关概念、工作流或笔记的问题。答案提炼自一个私有语料库，已炼化 201 篇论文和 47 篇学位论文，涵盖可预报性、目标观测、集合预报与非线性优化方法。</p>
</section>

<section class="cnop-ask-notice" aria-label="Usage notice">
<p><strong>由于技术限制，仅能抽取最相关的6篇学术论文和3篇学位论文，并基于 DeepSeek V4 Pro 回答。建议在提问时写出关键词以获得更好的匹配效果。</strong></p>
<p><strong>Due to technical constraints, only the 6 most relevant published papers and 3 theses are retrieved, and answers are generated by DeepSeek V4 Pro. Include keywords in your question for better matching.</strong></p>
<p><strong>AI 生成内容仅供参考。 AI-generated content is for reference only.</strong></p>
<p><strong>注意：您的提问文本将展示在下方，请不要输入敏感信息。下方仅记录最近 5 个问题，更早的将移除。</strong></p>
<p><strong>Note: Your question text will be displayed below. Do not enter sensitive information. Only the latest 5 questions are kept; older ones are removed.</strong></p>
</section>

<section class="cnop-ask-cost" id="cnop-cost-bar" aria-label="Cumulative cost">
<span class="cnop-ask-cost__label">累计消耗 / Total spent</span>
<span class="cnop-ask-cost__value" id="cnop-cost-value">¥0.000000</span>
</section>

<section class="cnop-ask-panel" aria-label="CNOP question form">
<form class="cnop-ask-form" id="cnop-ask-form">
<label for="cnop-question">Question</label>
<textarea id="cnop-question" name="question" rows="7" placeholder="Ask something about CNOP..." required></textarea>
<div class="cnop-ask-actions">
<button type="submit" id="cnop-submit">Ask</button>
<button type="button" id="cnop-clear">Clear</button>
</div>
<div class="cnop-ask-suggest" id="cnop-suggest" aria-label="Suggested questions">
<p class="cnop-ask-suggest__label">试试这样问 / Try asking:</p>
</div>
</form>

<div class="cnop-ask-answer" id="cnop-answer" aria-live="polite">
<div class="cnop-ask-answer__head">
<p class="cnop-ask-answer__label">Answer</p>
<button type="button" class="cnop-ask-copy" id="cnop-copy" hidden>Copy</button>
</div>
<p class="cnop-ask-answer__text">Submit a question to see the response here.</p>
<div class="cnop-ask-keywords" id="cnop-keywords" hidden></div>
</div>
</section>

<section class="cnop-ask-recent" aria-label="Recent questions">
<h2 class="cnop-ask-recent__title">看看大家最近的问题 / Recent questions</h2>
<div class="cnop-ask-recent__list" id="cnop-recent-list">
<p class="cnop-ask-recent__empty">Loading…</p>
</div>
</section>
</div>

<script>
(() => {
  const endpoint = "https://summer-bar-0fc8.zhuangyi17.workers.dev/";
  const form = document.getElementById("cnop-ask-form");
  const question = document.getElementById("cnop-question");
  const submit = document.getElementById("cnop-submit");
  const clear = document.getElementById("cnop-clear");
  const answer = document.getElementById("cnop-answer");
  const answerText = answer.querySelector(".cnop-ask-answer__text");
  const copyBtn = document.getElementById("cnop-copy");
  const keywordsBox = document.getElementById("cnop-keywords");

  let timerInterval = null;

  function setAnswer(text, state = "idle", kws) {
    answer.dataset.state = state;
    answerText.textContent = text;
    copyBtn.hidden = state !== "success";
    copyBtn.classList.remove("copied");
    copyBtn.textContent = "Copy";
    if (kws && kws.length > 0) {
      keywordsBox.innerHTML = '<span class="cnop-ask-keywords__label">Keywords:</span> ' + kws.map(k => `<span class="cnop-ask-keyword">${k}</span>`).join("");
      keywordsBox.hidden = false;
    } else {
      keywordsBox.hidden = true;
    }
  }

  function startLoading() {
    let seconds = 0;
    const messages = [
      "正在检索语料库… / Retrieving corpus…",
      "正在匹配相关论文… / Matching relevant papers…",
      "正在生成回答… / Generating answer…",
      "仍在生成，请稍候… / Still generating, please wait…",
    ];
    setAnswer(messages[0], "loading");
    timerInterval = setInterval(() => {
      seconds++;
      const idx = Math.min(Math.floor(seconds / 4), messages.length - 1);
      const suffix = seconds >= 8 ? `（${seconds}s）` : "";
      setAnswer(messages[idx] + suffix, "loading");
    }, 1000);
  }

  function stopLoading() {
    if (timerInterval) { clearInterval(timerInterval); timerInterval = null; }
  }

  form.addEventListener("submit", async (event) => {
    event.preventDefault();

    const value = question.value.trim();
    if (!value) {
      setAnswer("Please enter a question before submitting.", "error");
      question.focus();
      return;
    }

    submit.disabled = true;
    startLoading();

    const controller = new AbortController();
    const timeoutId = setTimeout(() => controller.abort(), 60000);

    try {
      const response = await fetch(endpoint, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ question: value }),
        signal: controller.signal,
      });

      clearTimeout(timeoutId);

      if (response.status === 429) {
        const data = await response.json().catch(() => ({}));
        setAnswer(data.error || "今日额度已用完。/ Today's quota has been used up.", "error");
        return;
      }

      if (!response.ok) {
        const data = await response.json().catch(() => ({}));
        throw new Error(data.error || `HTTP ${response.status}`);
      }

      const data = await response.json();
      stopLoading();
      let text = data.answer || "The worker returned no answer.";
      if (typeof data.remaining === "number") {
        text += `\n\n（今日剩余 ${data.remaining} 次 / ${data.remaining} calls remaining today）`;
      }
      setAnswer(text, "success", data.keywords);
      loadRecent();
      if (typeof data.totalCost === "number") {
        document.getElementById("cnop-cost-value").textContent = "¥" + data.totalCost.toFixed(6);
      }
    } catch (error) {
      clearTimeout(timeoutId);
      stopLoading();
      if (error.name === "AbortError") {
        setAnswer("请求超时（60秒），请稍后重试。/ Request timed out (60s). Please try again later.", "error");
      } else {
        setAnswer("请求失败：" + error.message + " / Request failed: " + error.message, "error");
      }
    } finally {
      stopLoading();
      submit.disabled = false;
    }
  });

  clear.addEventListener("click", () => {
    question.value = "";
    setAnswer("Submit a question to see the response here.");
    question.focus();
  });

  copyBtn.addEventListener("click", async () => {
    const text = answerText.textContent;
    try {
      await navigator.clipboard.writeText(text);
      copyBtn.classList.add("copied");
      copyBtn.textContent = "Copied!";
      setTimeout(() => {
        copyBtn.classList.remove("copied");
        copyBtn.textContent = "Copy";
      }, 2000);
    } catch {
      copyBtn.textContent = "Failed";
      setTimeout(() => { copyBtn.textContent = "Copy"; }, 2000);
    }
  });

  const recentList = document.getElementById("cnop-recent-list");

  function timeAgo(ts) {
    const diff = Date.now() - ts;
    const m = Math.floor(diff / 60000);
    if (m < 1) return "刚刚 / just now";
    if (m < 60) return `${m} 分钟前 / ${m}m ago`;
    const h = Math.floor(m / 60);
    if (h < 24) return `${h} 小时前 / ${h}h ago`;
    const d = Math.floor(h / 24);
    return `${d} 天前 / ${d}d ago`;
  }

  async function loadRecent() {
    try {
      const r = await fetch(endpoint + "api/recent", { method: "GET" });
      if (!r.ok) throw new Error(`HTTP ${r.status}`);
      const data = await r.json();
      const items = data.recent || [];
      if (items.length === 0) {
        recentList.innerHTML = '<p class="cnop-ask-recent__empty">还没有人提问 / No questions yet.</p>';
        return;
      }
      recentList.innerHTML = "";
      items.forEach((it, idx) => {
        const details = document.createElement("details");
        details.className = "cnop-ask-recent__item";
        const summary = document.createElement("summary");
        summary.className = "cnop-ask-recent__summary";
        const qSpan = document.createElement("span");
        qSpan.className = "cnop-ask-recent__q";
        qSpan.textContent = it.q;
        const tSpan = document.createElement("span");
        tSpan.className = "cnop-ask-recent__time";
        tSpan.textContent = timeAgo(it.ts);
        summary.appendChild(qSpan);
        summary.appendChild(tSpan);
        details.appendChild(summary);

        const body = document.createElement("div");
        body.className = "cnop-ask-recent__body";
        const ansP = document.createElement("p");
        ansP.className = "cnop-ask-recent__ans";
        ansP.textContent = it.a || "(no answer stored)";
        const copyB = document.createElement("button");
        copyB.type = "button";
        copyB.className = "cnop-ask-copy cnop-ask-copy--sm";
        copyB.textContent = "Copy";
        copyB.addEventListener("click", async () => {
          try {
            await navigator.clipboard.writeText(ansP.textContent);
            copyB.classList.add("copied");
            copyB.textContent = "Copied!";
            setTimeout(() => { copyB.classList.remove("copied"); copyB.textContent = "Copy"; }, 2000);
          } catch {
            copyB.textContent = "Failed";
            setTimeout(() => { copyB.textContent = "Copy"; }, 2000);
          }
        });
        body.appendChild(ansP);
        body.appendChild(copyB);
        details.appendChild(body);
        recentList.appendChild(details);
      });
    } catch {
      recentList.innerHTML = '<p class="cnop-ask-recent__empty">加载失败 / Failed to load.</p>';
    }
  }

  loadRecent();
  setInterval(loadRecent, 30000);

  async function loadCost() {
    try {
      const r = await fetch(endpoint + "api/cost");
      if (r.ok) {
        const d = await r.json();
        if (typeof d.totalCost === "number") {
          document.getElementById("cnop-cost-value").textContent = "¥" + d.totalCost.toFixed(6);
        }
      }
    } catch {}
  }
  loadCost();

  // --- Daily suggested questions ---
  const SUGGESTIONS = [
    "什么是CNOP？它与传统的奇异向量(SV)有什么本质区别？",
    "CNOP如何用于识别ENSO预报中最容易导致预测失败的初始误差？",
    "春季可预报性障碍(SPB)是什么？CNOP如何帮助理解其机制？",
    "CNOP和SV在热带气旋路径预报中各自的优势和局限性是什么？",
    "目标观测(target observation)的敏感区如何通过CNOP方法确定？",
    "CNOP-P和CNOP有什么区别？它们分别用于研究什么问题？",
    "什么是NFSV和C-NFSV？它们与CNOP的关系是什么？",
    "CNOP方法如何用于集合预报的初始扰动设计？",
    "El Nino和La Nina振幅不对称性的非线性机制是什么？",
    "CNOP在ENSO最优前期征兆(optimal precursor)研究中的应用",
    "如何用CNOP方法评估模式参数误差对预报的影响？",
    "春季可预报性障碍与初始误差的哪种空间结构最相关？",
    "CNOP与Lyapunov向量在可预报性研究中的区别",
    "两类El Nino事件(EP型和CP型)的可预报性有何不同？",
    "CNOP方法如何帮助改进台风目标观测试验的设计？",
    "什么是O-CNOPs？它在集合预报中如何应用？",
    "ENSO预报中的模式误差和初始误差哪个更重要？",
    "CNOP方法在IOD(印度洋偶极子)可预报性研究中的应用",
    "非线性温度平流如何影响El Nino和La Nina的不对称性？",
    "CNOP如何用于评估观测对预报改进的实际效果？",
    "春季预报障碍是否与季节依赖的非线性误差增长有关？",
    "MJO对ENSO可预报性有什么影响？",
    "CNOP方法在黑潮可预报性研究中的应用",
    "粒子滤波同化在ENSO目标观测中的作用",
    "CNOP在热盐环流(THC)敏感性分析中的应用",
    "AI-CNOP是什么？深度学习如何改进CNOP计算？",
    "FuXi-En4DVar如何结合AI资料同化与CNOP方法？",
    "极端低温预报的不确定性主要来自初始条件还是模式误差？",
    "C-NFSVs如何用于WRF集合预报的我国南方2m温度预报？",
    "CNOP方法如何评估ECMWF集合成员的选择效果？",
    "Zebiak-Cane模式中CNOP揭示了什么ENSO可预报性特征？",
    "CNOP与BV(Bred Vector)在集合扰动生成中的差异",
    "CNOP如何用于评估海表温度(SST)预报的敏感性？",
    "非线性最优误差增长的季节依赖性对ENSO预报意味着什么？",
    "CNOP方法在NAO(北大西洋涛动)可预报性研究中有何应用？",
    "如何用CNOP方法研究大气污染预报的可预报性？",
    "CNOP在陆地生态系统可预报性研究中的应用",
    "条件非线性最优扰动方法的发展历史和关键里程碑",
    "CNOP与变分资料同化的关系是什么？",
    "如何评估CNOP方法的计算效率和准确性？",
    "CNOP方法在WRF/WRFPLUS台风集合预报中的应用",
    "次季节到季节(S2S)尺度上CNOP的应用前景",
    "CNOP如何用于识别最敏感的观测区域以改进预报？",
    "模式参数化方案对CNOP结果的影响有多大？",
    "CNOP方法如何帮助理解混沌系统中的可预报性极限？",
    "基于CNOP的敏感区识别与传统奇异向量方法的结果有何不同？",
    "CNOP在厄尔尼诺事件可预报性研究中的最新进展",
    "如何将CNOP方法应用于AI驱动的天气预报大模型？",
    "CNOP方法在未来气候预测中的挑战和机遇",
  ];

  const suggestBox = document.getElementById("cnop-suggest");

  function dailySeed() {
    const d = new Date();
    return d.getFullYear() * 10000 + (d.getMonth() + 1) * 100 + d.getDate();
  }

  function seededPick(arr, n, seed) {
    const copy = [...arr];
    const result = [];
    let s = seed;
    for (let i = 0; i < n && copy.length > 0; i++) {
      s = (s * 1103515245 + 12345) & 0x7fffffff;
      const idx = s % copy.length;
      result.push(copy.splice(idx, 1)[0]);
    }
    return result;
  }

  function renderSuggestions() {
    const picks = seededPick(SUGGESTIONS, 3, dailySeed());
    suggestBox.innerHTML = '<p class="cnop-ask-suggest__label">试试这样问 / Try asking:</p>';
    picks.forEach((q) => {
      const row = document.createElement("div");
      row.className = "cnop-ask-suggest__item";
      const text = document.createElement("span");
      text.className = "cnop-ask-suggest__text";
      text.textContent = q;
      const btn = document.createElement("button");
      btn.type = "button";
      btn.className = "cnop-ask-suggest__btn";
      btn.textContent = "→";
      btn.title = "Copy to question box";
      btn.addEventListener("click", () => {
        question.value = q;
        question.focus();
      });
      row.appendChild(text);
      row.appendChild(btn);
      suggestBox.appendChild(row);
    });
  }

  renderSuggestions();
})();
</script>
